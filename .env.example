# demo = scripted responses + local JSON; live = OpenAI + MongoDB (both required).
APP_MODE=demo
PORT=3001
HOST=127.0.0.1
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
MONGODB_URI=mongodb://127.0.0.1:27017
MONGODB_DB=cadence
